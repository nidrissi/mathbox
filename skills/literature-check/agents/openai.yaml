interface:
  display_name: "Literature Check"
  short_description: "Verify a mathematical source and implication"
  default_prompt: "Use $literature-check to verify the exact source, hypotheses, and implication."
policy:
  allow_implicit_invocation: true
